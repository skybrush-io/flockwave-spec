{
    "type": "object",
    "properties": {
        "type": {
            "title": "Type",
            "description": "Type of the message",
            "type": "string",
            "enum": ["CMD-DEL"]
        },
        "ids": {
            "$ref": "definitions.json#/receiptIdList"
        }
    },
    "required": [
        "type", "ids"
    ],
    "additionalProperties": false
}
